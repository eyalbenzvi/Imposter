# TRANSPORT_PLAN.md — תיקון שכבת החיבורים

**גרסה 2** — אחרי ביקורת שנייה. סעיף 6 מסכם מה השתנה ולמה.

**האבחנות נכונות, שלוש מהתרופות לא היו.** הביקורת השנייה אימתה כל טענה על
PeerJS מול המקור, אישרה את ארבעת הכשלים, ופסלה שלוש מהתרופות כגרועות מהבאג
שהן מחליפות. הגרסה הזאת היא רשימת ביצוע, לא מסמך דיון.

**האילוץ לא משתנה:** אפס עלות, אין שירותי צד שלישי בתשלום. נשארים עם PeerJS.

---

## 0. עובדות שאומתו מול `node_modules/peerjs/dist/bundler.mjs`

| עובדה | היכן |
|---|---|
| ברירת המחדל כוללת **TURN** — `turn:eu-0/us-0.turn.peerjs.com` | `:132-137` |
| `options.token` נוצר פעם אחת ל-`Peer`; `reconnect()` משתמש בו מחדש. `new Peer(אותו id)` **לא** יכול לקחת id בחזרה, `reconnect()` כן | `:1783-1790` |
| `DataConnection.close()` פולט `'close'` **סינכרונית** | `:1209` |
| נפילת socket אחרי open → `_abort` → `disconnect()` ולא `destroy()`. הערוצים שורדים | `:1698-1714` |
| ⚠️ **`peer.reconnect()` זורק** גם על peer הרוס וגם על peer שאינו מנותק | `:1783-1795` |
| ⚠️ **`peer.destroy()` פולט `'disconnected'`** לפני שהוא מסמן `_destroyed` | `:1698-1714` |
| ⚠️ **`close({flush:true})` לא סוגר כלום מקומית.** הוא שולח סמן `__peerData:{type:'close'}` ויוצא. הצד השני סוגר את עצמו | `:1185-1192`, `:1283-1291` |
| `conn.close()` יוצא מוקדם בלי לפלוט כשה-`open` כבר שקרי | `:1208` |

שלוש האזהרות האחרונות הן מה שפסל את התרופות המקוריות שלי.

---

## 1. תיקוני התקשורת

### 🔴 1 — `wrap()`: שלושה דגלים, לא אחד

**`src/online/channel.ts`** (חדש — ראו §4)

דגל `closed` אחד עושה שתי עבודות. `close()` מסמן אותו לפני `conn.close()`,
ו-PeerJS פולט `'close'` סינכרונית, אז `fire()` יוצא ו-`onCloseCb` לא נקרא.
גלאי השתיקה אצל האורח קורא ל-`close()` כדי להתחיל התחברות מחדש — ובפועל
**מקפיא את המסך לצמיתות**, בדיוק בתרחיש שה-heartbeat נכתב בשבילו.

התרופה הראשונה שלי, `notified` יחיד, החזירה את ה-commit הכפול מדלת אחרת:
`close()` סימן `notified`, ואז `onClose` שנרשם אחר כך היה יורה מיד.

```ts
let silenced = false;   // אנחנו סגרנו — אסור להודיע לבעלים
let fired    = false;   // כבר הודענו
let pending  = false;   // נסגר לפני שמישהו נרשם

const fire = () => {
  if (fired || silenced) return;
  fired = true;
  if (onCloseCb) onCloseCb(); else pending = true;
};

close()           { silenced = true; fired = true; try { conn.close(); } catch {} }
closeGracefully() { silenced = true; fired = true; try { conn.close({ flush: true }); } catch {} }
drop()            { try { conn.close(); } catch {} fire(); }   // בלי דגל מקדים
onClose(cb)       { onCloseCb = cb; if (pending) { pending = false; cb(); } }
isOpen()          { return !silenced && !fired && conn.open; }
```

`drop()` **חייב** לקרוא ל-`fire()` מפורשות: `conn.close()` יוצא מוקדם בלי לפלוט
כשהחיבור כבר סגור.

- `useGuest` → `drop()`. המארח נשאר על `close()` (הוא עושה commit בעצמו).
- `closeGracefully()` משתיק את העטיפה למרות ש-`conn.open` עדיין אמת.

### 🔴 2 — לולאת הברוקר: טיימר יחיד, בר-ביטול

**`src/online/brokerLoop.ts`** (חדש) + `peer.ts`

האבחנה: נפילת ה-socket לברוקר משאירה את הערוצים חיים ואת ה-id בלתי-נתיב. אין
בקוד שום `peer.on('disconnected')`, וכל שגיאה שאחרי ה-open נבלעת.

התרופה המקורית שלי הייתה מסוכנת משלושה כיוונים, וכולם מאותו snippet:

1. הסרתי את השמירה `if (peer.disconnected)` — ו-`reconnect()` **זורק** גם על
   peer הרוס וגם על peer מחובר.
2. `peer.destroy()` פולט `'disconnected'`, אז **סגירת חדר הייתה מדריכה את
   הלולאה נגד גופה**. ומלכודת סדר: בתוך ה-handler `peer.destroyed` עדיין
   `false`, אז שמירה שם לא עוזרת — היא חייבת להיות ב-callback של הטיימר.
3. `_abort` עושה `emitError` ואז `disconnect`, ושני ה-handlers קראו ל-`nudge()`
   — שתי שרשראות טיימר לכל כשל. ומכיוון שרשמתי אותם בתוך `tryOpen`, לולאת
   ה-`unavailable-id` הייתה מייצרת **~25 לולאות מקבילות שכולן זורקות**, עוד
   לפני שהלובי נפתח.

הלולאה מחולצת למכונת מצבים טהורה עם IO מוזרק:

```ts
export type BrokerLoop = { down(): void; up(): void; stop(): void };

export function makeBrokerLoop(io: {
  reconnect(): void;          // מותר לו לזרוק — הלולאה חייבת לשרוד
  isDead(): boolean;
  isDisconnected(): boolean;
  setTimeout(fn: () => void, ms: number): number;
  clearTimeout(id: number): void;
  onState(s: 'UP' | 'DOWN'): void;
}): BrokerLoop;
```

- טיימר אחד. `down()` פעמיים דורך פעם אחת.
- `up()` מבטל ומאפס backoff ל-1s.
- `stop()` מבטל, וכל `down()` אחריו הוא no-op.
- השמירות (`isDead`, `isDisconnected`) ב-callback של הטיימר.
- `reconnect()` שזורק נתפס ולא הורג את הלולאה.
- תקרה: אחרי ~2 דקות של כישלון רצוף — מצב סופי, לא ספין נצחי ב-20s.

ב-`peer.ts`: ה-handlers נרשמים **פעם אחת בתוך ה-`'open'` הראשון**, מאחורי
`if (opened) return`. בלעדיו חיבור מחדש מוצלח רושם handler שני ל-`'connection'`
וכל הודעה מעובדת פעמיים — שתי הצבעות מלחיצה אחת.

`HostPeer.reconnect()` **שומר** את `if (peer.disconnected && !peer.destroyed)`.
ה-visibility handler קורא גם לו וגם ל-`nudge()`: הטענה על iOS נכונה (ה-
`onclose` מגיע אחרי ה-`visibilitychange`), אבל התשובה היא לדרוך את הלולאה, לא
להסיר את השמירה.

### 🔴 3 — לחשוף את מצב הברוקר

`host.status` נקבע ל-`'OPEN'` פעם אחת ולעולם לא חוזר, אז הבאנר הקיים הוא קוד מת.

- `HostStatus` מקבל `'DEGRADED'`.
- **`onBroker` הוא setter על `HostPeer`, לא אופציה ל-`openHost`.** `openHost`
  מחזיר promise מה-cache, אז אופציה של הקורא השני נזרקת בשקט — בדיוק הבאג
  ש-`onConnect` הפך ל-setter כדי להימנע ממנו. הוא גם משחזר את המצב הנוכחי
  למי שנרשם מאוחר.
- הבאנר מותנה ב-`status === 'DEGRADED'` **מפורשות**, לא ב-`!== 'OPEN'` —
  אחרת הטקסט "המשחק ממשיך" יופיע על חדר שמעולם לא נפתח.

### 🔴 4 — הביטול ב-`openHost`

`if (cancelled) return;` ב-`useHost`. זהו.

התרופה השנייה שהצעתי — אימות cache עם `isDead()` — היא **קוד מת**: אחרי
התיקון הזה שום דבר לא הורס את ה-peer חוץ מ-`destroyHost()`, שמאפס את
`live`. ואם היא כן הייתה נורית, היא הייתה קוראת ל-`openHost` עם אותו
`preferredCode`, ש-`new Peer` לא יכול לקחת בחזרה. נשמרת רק כ-assertion.

### 🟠 5 — שני תקציבים, ו**`seatedOnce` גובר על `NO_ROOM`**

**`src/online/retry.ts`** (חדש) + `useGuest.ts`

```ts
const MAX_DIAL_ATTEMPTS   = 1;
const RECONNECT_BUDGET_MS = 90_000;
const BACKOFF_MS = [1_000, 2_000, 4_000, 6_000, 6_000, 6_000];
```

**החור שהתרופה המקורית השאירה, והוא הרצף הכי סביר במסמך כולו:** תיקון 2 מתאר
בדיוק את המצב שבו ה-socket של המארח נופל ואורח שמנסה מקבל `NO_ROOM`. אבל אורח
**שכבר יושב** שהערוץ שלו נפל באותו חלון עובר `giveUp('NO_ROOM')` → **מנקה את
הסשן** → "אין חדר עם הקוד הזה". המושב שלו עדיין בחדר. תקציב ה-90 שניות לא
מקבל הזדמנות לרוץ, כי הוויתור על הסיווג ולא על המונה. ואחרי שהסשן נמחק,
הפעלה מחדש כבר לא מחזירה אותו לחדר בכלל.

```ts
if (seatedOnce) {
  // NO_ROOM כאן פירושו שה-socket של המארח נפל, לא שהחדר נעלם.
  if (now - dropAt > RECONNECT_BUDGET_MS) giveUp(why);
} else if (why === 'NO_ROOM' || attempt >= MAX_DIAL_ATTEMPTS) {
  giveUp(why);
}
```

ו-`giveUp` מנקה סשן **רק** כש-`!seatedOnce`.

ב-`peer.ts`: `RETAIN_MS` → `75_000`, `RETRY_STEP_MS` → `3_000`, ומונה נפרד
ל-`unavailable-id` על הקוד המועדף.

**אבל החלפת קוד שקטה נפסלת.** כשקוד מועדף לא ניתן לשחזור, הקוד הנוכחי היה
מגריל חדש ומעדכן את הלובי — בזמן שכל האורחים מחייגים לישן וה-QR ששותף מצביע
עליו. במקום: `ConnectError('CODE_LOST')` ומסך שאומר "קוד החדר הישן אבד —
פתחו חדר חדש והקריאו את המספר החדש", עם כפתור שמנקה את הסשן. הגרלה חוזרת
נשארת רק ל-open **טרי** (התנגשות עם חדר של זר).

### 🟠 6 — התאוששות מרקע אצל האורח

```ts
const onVisible = () => {
  if (document.visibilityState !== 'visible') return;
  if (stopped.current) return;          // ⚠️ CLOSED/REJECTED/UNREACHABLE נשארים
  const ch = channelRef.current;
  if (!ch || !ch.isOpen()) { attempt.current = 0; setNonce((n) => n + 1); return; }
  heardFrom.current = Date.now();       // ⚠️ בלי זה — שתיקה שקרית מיידית
  ch.send({ t: 'PING' });
};
```

השורה `if (stopped.current) return` היא תיקון לתרופה שלי: בלעדיה, אורח שיושב
על "החדר נסגר" ומחליף אפליקציה חוזר ומחייג מחדש לחדר שהמארח סגר במפורש —
כלומר הבאג שמסך `UNREACHABLE` נכתב כדי להרוג, מוחזר.

### 🟠 7 — ערוצי רפאים, **וסריקה שעמידה לחזרה מרקע**

`staleConnIds` עובר על `room.seats`, אז הוא יכול לפנות רק חיבור שמחזיק מושב.
בורחים ממנו: ערוץ שנדחה ומעולם לא קיבל מושב, וערוץ שהמושב שלו נלקח.

**אבל המעבר לסריקת טבלת הערוצים מסוכן בלי תיקון נוסף:** למארח יש בדיוק אותו
באג של קפיצת שעון שתיקון 6 מתקן לאורח, וה-`onVisible` שלו לא מרענן `lastSeen`.
אחרי דקה ברקע, הסריקה יורה פעם אחת עם `now - seen ≈ 60_000` **לכל ערוץ**
וסוגרת את כולם. בגרסת טבלת-הערוצים זה גרוע מהיום, שבו נסרקים רק מושבים.

```ts
const gap = now - lastSweepAt.current;
lastSweepAt.current = now;
if (gap > SILENCE_TIMEOUT_MS) {          // השעון קפץ — היינו ישנים
  for (const id of channels.current.keys()) lastSeen.current.set(id, now);
  return;                                 // מדלגים בדיוק על סבב אחד
}
```

וגם ה-`visibilitychange` של המארח מרענן `lastSeen` לפני ה-broadcast.

`staleConnIds` משנה חתימה ל-`(connIds: Iterable<string>, lastSeen, now, timeoutMs)`
בלי `Room` בכלל. כלל "לא לסרוק את המארח" עובר לקורא — ושם הוא נכון מעצם
הבנייה, כי ל-`connId: 'host'` אין ערך ב-`channels`.

**דחיית `REJECTED` חייבת סגירה מסודרת.** `send` ואז `close()` הוא בדיוק
הדפוס שתיקון 8 פוסל — ההודעה נשארת בחוצץ, האורח לא לומד למה נדחה, ומנסה שוב
בלולאה בלי הודעה. `closeGracefully()` ואז סגירה קשה נדחית ב-400ms.

**והאורח חייב להפסיק לפנג על דחייה סופית.** היום `stopped.current = true` אבל
`channelRef.current` נשאר, אז ה-heartbeat ממשיך לשלוח PING לנצח — מה ששומר את
`lastSeen` טרי והופך את הערוץ ל**בלתי-נסרק לצמיתות**.

**וערוץ שנעקר בהשתלטות על מושב נסגר מיידית.** המארח יודע את ה-connId הישן
ברגע ההשתלטות; אין טעם לחכות לשתיקה שלא תגיע כשהערוץ הישן דווקא חי.

### 🟠 8 — `CLOSED` שמגיע, ו-cache שלא נהרס תחת הקורא הבא

```
channel.send({ t: 'CLOSED' });
channel.close();               // הורס את ה-RTCPeerConnection סינכרונית
```

**תיקון לתיאור שלי:** `close({flush:true})` **לא** סוגר כלום מקומית ולא ממתין
לניקוז. הוא שולח סמן והצד השני סוגר את עצמו. **ההשהיה של 400ms היא כל
הניקוז**, ו-`flush` רק נותן לצד השני סגירה נקייה אחריה. בלי המשפט הזה מישהו
ימחק את ההשהיה כמיותרת.

```ts
export function destroyHost(afterMs = 0): void {
  const entry = live;
  live = null;                    // ⚠️ סינכרוני: אף קורא מאוחר לא יקבל אותו
  if (!entry?.peer) return;
  if (afterMs === 0) entry.peer.destroy();
  else window.setTimeout(() => entry.peer!.destroy(), afterMs);
}
```

בלי איפוס סינכרוני: המשתמש חוזר לבורר, פותח חדר חדש בתוך 400ms, `openHost`
מחזיר את ה-peer **הישן** מה-cache, והטיימר הורג אותו. הלובי מציג קוד שאי אפשר
להגיע אליו.

אותו טיפול ל-`leave()` של האורח — `closeGracefully()` ואז `destroyGuest`
בהשהיה, אחרת ה-`LEAVE` אובד והמארח מחזיק מושב פנטום 10 שניות.

---

## 2. תיקוני UX

### 9 — כפתור "שליטת מארח" מסתיר כפתורים

הכפתור `fixed` בתחתית מעל ה-`ScreenFooter`. עובר לפינה העליונה — לחריץ
ש-`ScreenHeader` **כבר שומר** (`pe-14`), אותו מקום שבו יושב כפתור הבית בסולו
ו-`LeaveButton` אצל אורח. המארח לא מרנדר `LeaveButton`, אז החריץ פנוי.

חפיפה נעשית בלתי אפשרית במקום להיות מנוהלת ב-z-index, ומבטלת את התיקון
מ-`ONLINE_PLAN §17.20`.

### 10 — שינוי הגדרות בין משחקים

`UPDATE_SETTINGS` חוקי רק ב-`SETUP`, והחדר נעול מרגע ההתחלה. הפתרון: פקודת
מארח `REOPEN` שמחזירה ללובי.

`BACK_TO_SETUP` תמיד חוקי (אין `requirePhase`) ומחזיר
`createInitialState(names, settings)` — שומר שמות והגדרות, ו-`startGame`
דורס אחר כך את הרשימה מ-`room.seats`. `seatOrderIsSound` ממשיך לעבור
(`!seatOrder → phase === 'SETUP'`).

**אבל "seats ללא שינוי" היה יוצר רפאים בני-אלמוות.** מושב שנותק במשחק שרד עם
`connId: null`; אחרי הפתיחה מחדש אין לו ערוץ, אז הסריקה לא תיגע בו לעולם,
ו-`startGame` יקפיא אותו לתוך `seatOrder`, יחלק לו תפקיד, ויספור אותו בכל סף.
במקרה הגרוע הוא המתחזה והמשחק לא יכול להסתיים.

```ts
case 'REOPEN': {
  if (state.phase !== 'GAME_OVER') return reject(room, 'NOT_ALLOWED');
  const out = commit(room, [{ type: 'BACK_TO_SETUP' }], env);  // מקדם epoch, מנקה pending
  if (!out.accepted) return out;
  return {
    room: {
      ...out.room,
      locked: false,
      seatOrder: null,
      // מי שלא באמת כאן לא מחולק לתוך המשחק הבא.
      seats: out.room.seats.filter((s) => s.isHost || s.connId !== null),
      version: out.room.version + 1,
    },
    accepted: true,
  };
}
```

קידום ה-`epoch` דרך `commit` הוא חובה: כוונת `READY` שנשארה באוויר מ-`GAME_OVER`
תגיע אחרי הפתיחה מחדש, ו-`handleIntent` דוחה כל דבר ב-SETUP עם `NOT_ALLOWED`
— שצובע באנר אדום. עם epoch חדש זה `STALE`, שהוא שקט ומתקן את עצמו.

**להעיר בקוד:** אחרי `REOPEN` החדר פתוח, ולכן ניתוק **מוחק** מושב במקום להחנות
אותו. תקציב 90 השניות של תיקון 5 שומר זהות רק כשהחדר נעול.

**חיווט:** ב-`HostStrip`'s `commandsFor('GAME_OVER')`, שם ה-wiring כבר קיים —
אבל **פטור מ-`ripe`**, כי 10 שניות ההמתנה נכונות לכפתורי חירום ושגויות
לפעולה מכוונת.

### 11 — שינוי כינוי

**היכן:** בלובי בלבד. השם חי ב-`state.players[].name` שבבעלות ה-reducer, ואין
בו `RENAME`. עם תיקון 10, "בין משחקים" הוא לובי.

התרופה המקורית שלי לא הייתה שורדת מגע עם המציאות, משתי סיבות בלתי תלויות:

1. **מסלול החיבור מחדש היה דורס אותה.** `handleJoin` עושה
   `name: room.locked ? s.name : normalize(msg.name)` — ורק במצב לא-נעול
   מותר לשנות שם, כלומר בדיוק המקרה שדורס. ו-`msg.name` מגיע מה-prop של
   `useGuest`, ש-`OnlineApp` קובע פעם אחת מ-`initialRole()`.
   **התיקון: `handleJoin` לא משנה שם על התאמת `seatId`, נקודה.** JOIN הוא
   "חזרתי", לא "קראו לי ככה". זה גם מסלק מחלקה שלמה של מרוצים.
2. **היא מעולם לא נכתבה לדיסק.** `saveHostSession` תלוי ב-
   `[worthKeeping, epoch, settings, seats.length]`. שינוי שם דרך `touch()`
   מקדם רק `version`. **התיקון: חתימת שמות בין התלויות.**

עוד שלושה פרטים שהתוכנית המקורית פספסה:

- `RENAME` **לא** עובר ב-`handleIntent` — הוא דוחה הכול ב-SETUP ודורש
  `playerIdOf` לא-null, שהוא null כש-`seatOrder` null. הוא מנותב
  ב-`useHost.onMessage` לצד `JOIN`/`LEAVE`/`PING`, וצריך ערך ב-`GUEST_TYPES`
  ומקרה ב-`parseGuestMessage`.
- **בלי אימות `key`** — שני שחקנים ששולחים באותו tick היו דוחים זה את זה
  בלי סיבה.
- **המארח משנה שם דרך `HostCommand`**, כי `host.act` עובר ב-`handleIntent`.
- ה-JOIN של האורח לוקח שם מ-`loadGuestSession(code)?.name ?? name`, לא
  מה-`Role` הקפוא.

השער עצמו אטום: `validateJoin` משתמש ב-`nameKey` — בדיוק המפתח ש-
`duplicateNameIndexes` משתמש בו ב-`START_GAME` — ו-`setPlayers` מפעיל רק
`normalize` שהוא נייטרלי ל-`nameKey`. ריק ו->14 מכוסים.

---

## 3. בדיקות — לחלץ את המכונה, לא את החשבון

`peer.ts`/`useHost.ts`/`useGuest.ts` הם אפס בדיקות, ושם כל הקריטיים.

**`reconnectDelay(attempt)` הוא החילוץ השגוי.** `Math.min(1000 * 2**n, 20_000)`
מעולם לא היה שגוי ולעולם לא יהיה. כל פגם בביקורת הזאת שנוגע ללולאה הוא
ב**מכונת המצבים סביבה**. חילוץ החשבון בודק את החלק שכבר היה תקין — בדיוק
הביקורת שהמסמך הזה מותח על הבדיקות הקיימות.

**החלטה: לא מוסיפים jsdom.** במקום זה, כל חלק קשה מחולץ לקובץ טהור בלי
`peerjs` ובלי DOM, ונבדק ב-node:

| קובץ חדש | מה בפנים | למה טהור |
|---|---|---|
| `channel.ts` | `Channel`, `RawConn`, `wrap()` | לא מייבא `peerjs`; `RawConn` הוא ממשק מינימלי שקל לזייף |
| `brokerLoop.ts` | `makeBrokerLoop(io)` | כל ה-IO מוזרק, כולל השעון |
| `retry.ts` | `nextRetry(state, why)` | החלטת התקציב, בלי React |
| `room.ts` | `staleConnIds(connIds, …)` | חתימה חדשה, בלי `Room` |

הבדיקות:

| # | בדיקה |
|---|---|
| 1 | `close()` **לא** מפעיל `onClose`; `drop()` כן |
| 2 | `onClose` שנרשם אחרי סגירה **מרוחקת** נקרא מיד; אחרי `close()` שלנו — לא |
| 3 | `drop()` יורה גם כשה-`conn` כבר סגור ולא פולט |
| 4 | לולאה: `down()` כפול דורך טיימר אחד · `up()` מבטל ומאפס · `stop()` מבטל וכל `down()` אחריו no-op |
| 5 | לולאה: `reconnect()` שזורק לא הורג אותה · `isDead()` מדלג לגמרי · `isDisconnected()` שקרי מדלג |
| 6 | תקציבים: חיוג ראשון נכשל מהר · **`NO_ROOM` על מושב קיים ממשיך לנסות** · 90 שניות ואז ויתור |
| 7 | סריקה: ערוץ שהשתתק נסרק, כולל **בלי מושב** · קפיצת שעון מדלגת על סבב אחד |
| 8 | `REOPEN`: מחזיר ל-SETUP · **זורק מושבים מנותקים** · מקדם epoch · מאפשר start נוסף עם `seatOrder` תואם |
| 9 | `RENAME`: מתקבל בלובי · נדחה בנעילה · נדחה על כפילות `nameKey` · **שורד `handleJoin` של חיבור מחדש** |
| 10 | `parseGuestMessage` מקבל `RENAME` תקין ודוחה מעוות |

---

## 4. סדר הביצוע

| שלב | תוכן |
|---|---|
| A | `channel.ts` (תיקון 1) + בדיקות 1–3 |
| B | `brokerLoop.ts` (תיקון 2) + בדיקות 4–5 · חיווט ב-`peer.ts` · תיקון 3 · תיקון 4 |
| C | `retry.ts` (תיקון 5) + בדיקה 6 · תיקון 6 |
| D | תיקון 7 + בדיקה 7 · תיקון 8 |
| E | תיקון 9 (UI) · תיקון 10 + בדיקה 8 · תיקון 11 + בדיקות 9–10 |

---

## 5. מה לא נעשה, ולמה

| | |
|---|---|
| **החלפת PeerJS** | לא מתקנת אף כשל — כולם בקוד שלנו. `Channel` נשאר תפר נקי, אז החלפה מאוחרת היא יום עבודה |
| **Cloudflare Durable Objects** | היחיד שהיה מסלק גם את הברוקר וגם את הטלפון כנקודות כשל, וקיים ב-free tier. rewrite גדול מכל המסמך יחד. לשקול רק אם הברוקר יתגלה כלא אמין **אחרי** התיקונים |
| **מיגרציית מארח** | מכניסה split-brain. במשחק שבו טלפון אחד יודע מי המתחזה, זה גרוע מהתקלה שהיא מונעת |
| **jsdom + React Testing Library** | החילוץ לקבצים טהורים בודק את החלקים הקשים בלי משקל התשתית |
| **לשמור `pending` ברענון מארח** | ⚠️ **בכוונה.** `epoch` כן נשמר, אז כוונות באוויר עדיין תקפות. `pending` נזרק, וזה מרפא את עצמו: `youVoted` ו-`waiting.youDone` נגזרים חיים, אז כולם רואים "טרם" ומקישים שוב. הוספתו ל-`HostSession` תחזיר replay של הצבעות בין רענונים |

---

## 6. מה השתנה אחרי הביקורת השנייה

| # | הממצא | התיקון |
|---|---|---|
| 1 | 🔴 `reconnect()` זורק, `destroy()` פולט `'disconnected'`, ו-`_abort` דורך פעמיים — התרופה שלי ייצרה ~25 לולאות זורקות | טיימר יחיד בר-ביטול, שמירות ב-callback, handlers פעם אחת ב-`'open'` |
| 2 | 🔴 `seatedOnce` לא גבר על `NO_ROOM` — אורח יושב היה מסולק ומנוקה בדיוק בכשל שתיקון 2 מתאר | הסיווג נדחה מפני התקציב; ניקוי סשן רק כשמעולם לא ישב |
| 3 | 🔴 סריקת טבלת הערוצים הייתה מפנה את כל החדר אחרי דקה ברקע אצל המארח | זיהוי קפיצת שעון + רענון `lastSeen` ב-`visibilitychange` |
| 4 | 🔴 400ms השהיה השאירו את ה-cache נתפס — חדר חדש בתוך החלון היה נהרס | `live = null` סינכרוני, רק ההריסה נדחית |
| 5 | 🔴 `notified` יחיד החזיר את ה-commit הכפול מדלת אחרת | שלושה דגלים: `silenced` / `fired` / `pending` |
| 6 | 🔴 `REOPEN` היה מייצר מושבי רפאים בני-אלמוות שמקבלים תפקיד | סינון `connId === null`, קידום epoch, ניקוי pending |
| 7 | 🔴 `RENAME` היה נדרס בחיבור מחדש ולא נכתב לדיסק | `handleJoin` לא משנה שם; חתימת שמות בתלויות; ניתוב ב-`onMessage`; בלי `key` |
| 8 | 🟠 החלפת קוד שקטה אחרי 75 שניות הייתה מנתקת את כל האורחים | `CODE_LOST` + מסך מפורש; הגרלה חוזרת רק ל-open טרי |
| 9 | 🟠 `REJECTED` + `close()` הוא בדיוק הדפוס שתיקון 8 פוסל | `closeGracefully` + סגירה נדחית; והאורח מפסיק לפנג |
| 10 | 🟠 ערוץ שנעקר בהשתלטות ועדיין חי לא נסרק לעולם | נסגר דטרמיניסטית ברגע ההשתלטות |
| 11 | 🟠 ה-`onVisible` שלי היה מחייה אורח שכבר קיבל "החדר נסגר" | `if (stopped.current) return` ראשון |
| 12 | 🟠 `onBroker` כאופציה ל-`openHost` נזרק בשקט לקורא השני | setter על `HostPeer`, עם שחזור מצב |
| 13 | 🟠 הבדיקות שתכננתי לא ניתנות לכתיבה — אין jsdom ו-`include` לא תופס `.tsx` | חילוץ לקבצים טהורים במקום להוסיף תשתית |
| 14 | 🟠 `reconnectDelay` הוא החילוץ השגוי | `makeBrokerLoop` — המכונה, לא החשבון |
| 15 | 🟡 סתירה: "`staleConnIds` נשאר טהור" מול sketch שלא קורא לו | חתימה חדשה בלי `Room` |
| 16 | 🟡 אימות ה-cache עם `isDead()` הוא קוד מת | assertion בלבד |
| 17 | 🟡 ל-`REOPEN` לא היה חיווט — `OnlineGame` לא יכול לשלוח `HostCommand` | ב-`HostStrip`, פטור מ-`ripe` |
| 18 | 🟡 `leave()` של האורח מאבד את ה-`LEAVE` מאותה סיבה | `closeGracefully` + השהיה |

---

## 7. סבב QA שני — מה נמצא אחרי הפיתוח

הביקורת השנייה בדקה תוכנית. הסבב הזה בדק **קוד**, וזה שינה את סוג הממצאים:
רובם נולדו מהתיקונים עצמם, ואחד — הראשון — היה שם מהיום הראשון והוחמר
דווקא בגלל תיקון נכון.

| # | חומרה | הממצא | התיקון |
|---|---|---|---|
| 1 | 🔴 | **`seatId` ב-JOIN לא אומת מעולם.** מזהי מושב הם `s0`…`s11` וקוד החדר מוקרא בקול ומודפס ב-QR. הודעה אחת בשם מושב של מישהו אחר, והמארח משיב לה ב-`projectView` של אותו מושב: כרטיס החשיפה, ומכאן המילה, ומכאן — בהשוואה בין שני מושבים — גם המתחזה. בנוסף הצבעה בשמו, ו(מאז שההדחה סופית) הוצאתו מהמשחק | `Seat.token` — סוד שמונפק פעם אחת ב-`WELCOME` ונשמר בסשן. `seatId` בלי ה-token המתאים לא אומר דבר |
| 2 | 🟠 | **שינוי שם שהתקבל לא נשמר לעולם.** התנאי השווה מול השם ה*מבוקש*, שאחרי אישור זהה לשם הסמכותי — כלומר בדיוק במקרה שדרש כתיבה. התוצאה: כל חיבור מחדש שלח שוב `RENAME`, ובמשחק נעול הוא נדחה ב-`NOT_ALLOWED` וצבע באנר אדום על מסך של שחקן שרק חזר | `guestName.ts` — שתי פונקציות טהורות עם בדיקות. ההשוואה מול מה ש*נכתב*, והבקשה נשלחת מ-`VIEW` (שיודע אם החדר נעול) ולא מ-`WELCOME` (שלא) |
| 3 | 🟡 | מארח ראשון נלחם 75 שניות על קוד אקראי שהגריל בעצמו, ואז מודיע ש"הקוד הקודם לא זמין" | `preferredCode` רק כשיש סשן שמור |
| 4 | 🟡 | `command()` הדליק שגיאה ולא כיבה אותה — שם תפוס אחד השאיר קו אדום ללובי כולו | `setError(null)` בהצלחה |
| 5 | 🟡 | אחרי ויתור הלולאה, ה-backoff נשאר בתקרה — הניסיון הראשון אחרי חזרה ללשונית היה 20 שניות משם | איפוס ל-`FIRST_DELAY_MS` במסלול הוויתור |
| 6 | ⚪ | גיליון שליטת המארח נסגר תחת האצבע בכל תור, כי היה תלוי ב-epoch | סגירה תלויה ב-phase; שעון החסד נשאר תלוי ב-epoch, וזה מכוון |
| 7 | ⚪ | `parseHostMessage` אימת `reason` אבל לא `on`, ו-`on` מכריע אם דחייה סופית | אימות מול `GUEST_TYPES` |

**מה נבדק ונמצא תקין:** `projectView` בכל השלבים ובשני מצבי הרמז (הדליפה
ב-1 היא *מי* שאלו, לא מה שהוחזר) · ספירת `epoch`/`version` · `foldActions`
הטרנזקציוני · `authorise` והמתחזה המודח · `electorate` בכל שלב · תקינות
`seatOrder` · שני ה-`visibilitychange` · singletons וה-StrictMode · המצב
של מכשיר יחיד, שלא נגענו בו.

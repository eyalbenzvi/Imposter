# TRANSPORT_PLAN.md — תיקון שכבת החיבורים

תוכנית ביצוע לתיקוני שכבת התקשורת, אחרי ביקורת מומחה. משלימה את
`ONLINE_PLAN.md` ולא מחליפה אותה.

**מסקנת הביקורת:** הארכיטקטורה תקינה — מארח כסמכות, כוונות פנימה תצוגות
החוצה, `driver` טהור, `epoch` כמפתח סנכרון. מה ששביר הוא **ניהול מחזור החיים
של החיבור**, וכל ארבעת הכשלים החמורים יושבים בשלושה קבצים שאין להם ולו בדיקה
אחת: `peer.ts`, `useHost.ts`, `useGuest.ts`.

**האילוץ לא משתנה:** אפס עלות, אין שירותי צד שלישי בתשלום. נשארים עם PeerJS —
החלפת הברוקר לא מתקנת אף אחד מהכשלים, כי כולם בקוד שלנו.

---

## 0. שלוש עובדות שהתבררו בביקורת

| | |
|---|---|
| **יש לנו TURN** | PeerJS מגיע עם `turn:eu-0.turn.peerjs.com` כברירת מחדל, בנוסף ל-STUN של גוגל. חלק מהחשש מ-NAT כבר מכוסה |
| **`new Peer(אותו id)` לא יכול לקחת בחזרה את ה-id של עצמו** | הטוקן נוצר לכל instance, והשרת נותן id תפוס רק למי שהטוקן שלו תואם. רק `peer.reconnect()` — שמשתמש באותו טוקן — מצליח |
| **`peerjs-server` מחזיק id 60 שניות** אחרי ניתוק לא נקי | ה-`RETAIN_MS = 45_000` הנוכחי קצר מזה, כלומר מובטח כישלון |

---

## 1. שמונת התיקונים

### 🔴 תיקון 1 — `wrap()`: להפריד "אנחנו סוגרים" מ"זה נסגר"

**הקובץ:** `src/online/peer.ts:81-128`, `src/online/useGuest.ts`

דגל `closed` אחד עושה שתי עבודות. `close()` מסמן אותו **לפני** `conn.close()`,
ו-PeerJS פולט `'close'` סינכרונית — אז `fire()` רואה `closed === true` ויוצא.
**`onCloseCb` לא נקרא בסגירה יזומה.**

וזה בדיוק מה שגלאי השתיקה אצל האורח עושה: הוא קורא ל-`close()` כדי להתחיל
התחברות מחדש, וההערה בקוד טוענת שזה מה שקורה. בפועל: `status` נשאר `PLAYING`,
אין באנר, אין backoff, וה-interval יוצא מוקדם לנצח כי `isOpen()` שקרי. **מסך
קפוא לצמיתות**, בדיוק בתרחיש שה-heartbeat נכתב בשבילו.

```ts
let closed = false;
let notified = false;                    // האם בעל הערוץ קיבל הודעה?

const fire = () => {
  if (notified) return;
  notified = true;
  closed = true;
  onCloseCb?.();
};

close()  { closed = true; notified = true; try { conn.close(); } catch {} }  // שקט
drop()   { closed = true; try { conn.close(); } catch {} fire(); }           // ומודיע
onClose(cb) { onCloseCb = cb; if (notified) cb(); }   // סגירה שקדמה לרישום
```

- `Channel` מקבל `drop(): void`.
- `useGuest` קורא ל-`drop()` במקום `close()`.
- `onClose` שנרשם אחרי סגירה קורא ל-callback מיד — אחרת סגירה שנופלת בין
  `wrap()` לרישום נעלמת והאורח תקוע ב-`JOINING` בלי timeout.
- **המארח נשאר על `close()`** — הוא עושה את הניקוי בעצמו ב-sweep, ו-`drop()`
  היה גורם ל-commit כפול.

### 🔴 תיקון 2 — מחזור החיים של ה-socket לברוקר

**הקובץ:** `src/online/peer.ts:223-266`

כשה-WebSocket לברוקר נופל, PeerJS **מנתק** את ה-Peer ולא הורס אותו: ה-
DataChannels הקיימים שורדים, ואין חיבור מחדש אוטומטי. בקוד שלנו אין שום
`peer.on('disconnected')`, וכל שגיאה שאחרי ה-open נבלעת ב-`if (settled) return`.

התוצאה: **שני שחקנים ממשיכים לשחק, המארח מציג קוד תקין, ואף אורח חדש לא יכול
להתחבר.** אורח שמנסה מקבל `peer-unavailable` → מסווג `NO_ROOM` → "אין חדר עם
הקוד הזה". זה ההסבר לסימפטומים שדווחו, כולל "גם מדפדפן אחר" — התקלה בצד
המארח, אז הדפדפן של האורח לא רלוונטי.

```ts
peer.on('open', () => {
  window.clearTimeout(timer);
  backoff = 1_000;
  onBroker?.('UP');
  if (opened) return;          // ⚠️ קריטי — ראו למטה
  opened = true; settled = true;
  peer.on('connection', (conn) => conn.on('open', () => onConnectCb?.(wrap(conn))));
  resolve({ … });
});

peer.on('disconnected', () => { onBroker?.('DOWN'); nudge(); });
peer.on('error', (err) => {
  if (opened) { onBroker?.('DOWN'); nudge(); return; }
  … הטיפול הקיים שלפני ה-open …
});
```

`nudge()` = לולאת `peer.reconnect()` בהשהיה מוכפלת (1s → 20s), נעצרת כשה-
`'open'` חוזר.

**ה-`if (opened) return` הוא load-bearing:** בלעדיו חיבור מחדש מוצלח רושם
handler שני ל-`'connection'`, כל אורח עובר `wrap()` פעמיים, וכל הודעה מעובדת
פעמיים — שתי הצבעות מלחיצה אחת. הבאג הזה לא קיים היום **רק** מפני שחיבור מחדש
אף פעם לא עובד.

גם: להסיר את השמירה `if (peer.disconnected)` מ-`HostPeer.reconnect` ולקרוא
ל-`nudge()` מה-visibility handler. הסיבה: כש-iOS מקפיא טאב, אירוע ה-`onclose`
מגיע **בחזרה**, לרוב אחרי ש-`visibilitychange` כבר ירה — ואז `disconnected`
עדיין `false`, השמירה נכשלת, והקריאה היא no-op.

### 🔴 תיקון 3 — לחשוף את מצב הברוקר

**הקבצים:** `peer.ts` (`onBroker`), `useHost.ts`, `OnlineApp.tsx:224`

`host.status` נקבע ל-`'OPEN'` פעם אחת ולעולם לא חוזר. הבאנר שכבר כתוב תלוי
ב-`status !== 'OPEN'` והוא **קוד מת**.

- `HostStatus` מקבל `'DEGRADED'`.
- `onBroker('UP'|'DOWN')` מזיז אותו.
- הבאנר אומר את האמת: **"המשחק ממשיך — אבל אי אפשר לצרף שחקנים חדשים כרגע"**.
- בלובי, כשהמצב DEGRADED, הקוד עצמו מסומן כלא-פעיל: אין טעם להקריא מספר
  שהברוקר לא ינתב אליו.

### 🔴 תיקון 4 — `openHost`: הביטול הורס את ה-peer שהמאונט השני משתמש בו

**הקבצים:** `useHost.ts:269-278`, `peer.ts:174-200`

ב-StrictMode: אפקט 1 קורא ל-`openHost`, ה-cleanup מסמן `cancelled₁`, אפקט 2
מקבל **את אותו promise**. כשהוא נפתר, ה-callback של אפקט 1 רואה `cancelled₁`
וקורא ל-`destroyHost()` — ואפקט 2 ממשיך לעבוד מול peer הרוס. הלובי מציג קוד
של peer שה-id שלו כבר שוחרר.

זה סותר את המדיניות שכתובה שתי מסכים למטה באותו קובץ ("`closeRoom` הוא
הפירוק האמיתי"), וזה כנראה למה **מעולם לא הצלחנו לבדוק את זרימת המארח
ב-`npm run dev`**.

- `useHost.ts:271` → `if (cancelled) return;` בלבד.
- `openHost` מאמת את ה-cache: `if (live && live.peer?.isDead() !== true) return live.promise;` עם `isDead: () => peer.destroyed`.
- **`disconnected` הוא לא `dead`** — בשביל זה יש את לולאת ההתאוששות מתיקון 2.

### 🟠 תיקון 5 — שני תקציבי ניסיונות במקום אחד

**הקבצים:** `useGuest.ts:57-134`, `peer.ts:161-162`

התקציב הנוכחי (~13 שניות) נכון לחיוג ראשון לחדר שלא קיים, ושגוי לחלוטין
לחיבור מחדש באמצע משחק — שם צריך לשרוד 60 שניות של החזקת id בשרת ועוד רענון
מארח. ואחרי הוויתור, `useKeepAwake` מכבה והטלפון ננעל.

```ts
const MAX_DIAL_ATTEMPTS   = 1;        // מעולם לא התחברנו: להיכשל מהר
const RECONNECT_BUDGET_MS = 90_000;   // כבר היה מושב: לרדוף בשקט
const BACKOFF_MS = [1_000, 2_000, 4_000, 6_000, 6_000, 6_000];
```

`seatedOnce` נדלק ב-`WELCOME`. אם הוא דלוק — הוויתור לפי שעון קיר, לא לפי
מונה. הבאנר "מתחברים מחדש" נשאר על המסך כל הזמן.

ב-`peer.ts`: `RETAIN_MS` → `75_000` (מעל 60 השניות של השרת), `RETRY_STEP_MS`
→ `3_000`, וניסיונות `unavailable-id` על הקוד המועדף נספרים במונה נפרד כדי
שלא יפילו את bail-out ה-`attempt >= 6`.

### 🟠 תיקון 6 — לאורח אין התאוששות מרקע

**הקובץ:** `useGuest.ts`

למארח יש handler ל-`visibilitychange`, לאורח אין. אחרי השהיית טאב, ה-interval
חוזר, רואה ש-`heardFrom` עתיק, וקורא ל-`close()` — כלומר נופל בדיוק על תיקון 1.

```ts
const onVisible = () => {
  if (document.visibilityState !== 'visible') return;
  const ch = channelRef.current;
  if (!ch || !ch.isOpen()) { attempt.current = 0; setNonce((n) => n + 1); return; }
  heardFrom.current = Date.now();   // ⚠️ בלי זה — שתיקה שקרית מיידית
  ch.send({ t: 'PING' });
};
```

איפוס `heardFrom` הוא העיקר: בלעדיו רקע של דקה מייצר פסק דין "שתיקה" מיידי על
ערוץ חי לגמרי.

### 🟠 תיקון 7 — ערוצי רפאים

**הקבצים:** `useHost.ts:213-219`, `:338-354`

`staleConnIds` עובר על `room.seats`, ולכן יכול לפנות רק חיבור **שמחזיק מושב**.
שתי אוכלוסיות בורחות ממנו:

1. **ערוץ שמעולם לא קיבל מושב** — אורח שנדחה ב-`NAME_TAKEN`/`ROOM_FULL` נשאר
   ב-`channels` לנצח ומקבל PING כל 3 שניות.
2. **ערוץ שהמושב שלו נלקח** — `handleJoin` מעביר מושב ל-connId חדש בכוונה,
   והישן לא מוזכר יותר בשום מקום ולא נסגר.

הפתרון: **לסרוק את טבלת הערוצים, לא את רשימת המושבים.** `dropConnection` כבר
no-op בטוח ל-connId לא מוכר, אז לא נדרש שינוי ב-driver.

```ts
for (const [connId, channel] of channels.current) {
  const seen = lastSeen.current.get(connId) ?? now;
  if (now - seen <= SILENCE_TIMEOUT_MS) continue;
  channel.close();
  channels.current.delete(connId);
  lastSeen.current.delete(connId);
  next = dropConnection(next, connId);
}
```

בנוסף: לסגור את הערוץ מפורשות אחרי `REJECTED` סופי.

`staleConnIds` נשאר כפונקציה טהורה — הוא עדיין הכלל שנבדק, רק שהקורא מזין לו
את טבלת הערוצים.

### 🟠 תיקון 8 — `CLOSED` לא מגיע לאף אחד

**הקבצים:** `useHost.ts:444-449`, `peer.ts:112`

```ts
channel.send({ t: 'CLOSED', … });
channel.close();               // הורס את ה-RTCPeerConnection סינכרונית
```

ההודעה עדיין בחוצץ של SCTP. ל-PeerJS יש `close({ flush: true })` בדיוק בשביל
זה. התוצאה כיום: מסך "החדר נסגר" שכתבנו **בלתי נגיש**; במקומו כל אורח רואה
"החיבור אבד", שורף את תקציב הניסיונות, ונוחת על "לא הצלחנו להתחבר".

- `Channel.closeGracefully()` עם `conn.close({ flush: true })`.
- `closeRoom` שולח `CLOSED`, קורא ל-`closeGracefully()`, וממתין 400ms לפני
  `destroyHost()`. בלתי מורגש למארח, וכמה סיבובים על LAN.

---

## 2. שלושת תיקוני ה-UX

### תיקון 9 — כפתור "שליטת מארח" מסתיר כפתורים

**הקובץ:** `components/HostStrip.tsx:48-68`

הכפתור `fixed` בתחתית עם `z-[60]`, בדיוק מעל ה-`ScreenFooter` שבו יושבים
הכפתורים הראשיים.

**הפתרון:** להעביר אותו לפינה העליונה, לחריץ ש-`ScreenHeader` **כבר שומר**
(`pe-14`) — אותו מקום שבו יושב כפתור הבית במצב מכשיר-אחד, ושבו יושב
`LeaveButton` אצל אורח. המארח לא מרנדר `LeaveButton`, אז החריץ פנוי.

חפיפה נעשית בלתי אפשרית במקום להיות מנוהלת ב-z-index.

כשיש עקיפה זמינה או שהחדר תקוע, הכפתור מקבל נקודת התראה בצבע gold — עדיין
באותו מקום, בלי להזיז שום דבר.

### תיקון 10 — שינוי הגדרות בין משחקים

היום `UPDATE_SETTINGS` חוקי רק ב-`SETUP`, והחדר נעול מרגע ההתחלה. אחרי משחק
אפשר רק "סבב נוסף" עם אותן הגדרות בדיוק.

**הפתרון: פקודת מארח `REOPEN`, זמינה ב-`GAME_OVER`.**

```
BACK_TO_SETUP        ← תמיד חוקי; שומר שמות והגדרות
locked   = false
seatOrder = null     ← ייקפא מחדש בהתחלה הבאה
seats    ללא שינוי
```

החדר חוזר ללובי עם כולם עדיין יושבים, ההגדרות פתוחות לעריכה, ו"התחילו" מריץ
את אותו `startGame` הקיים. בונוס: מי שהגיע באיחור יכול להצטרף עכשיו, כי החדר
לא נעול.

- "סבב נוסף" נשאר הדרך המהירה, ללא שינוי.
- "שינוי הגדרות" הוא כפתור שני ב-`GAME_OVER`, למארח בלבד.
- האורחים רואים את מסך הלובי חוזר — זה כבר עובד, `projectView` מטפל ב-SETUP.

### תיקון 11 — שינוי כינוי

**היכן זה אפשרי:** בלובי בלבד. השם חי ב-`state.players[].name` שבבעלות
ה-reducer, ואין `RENAME` ב-`src/game/` — ושינוי שם באמצע משחק היה מחייב לגעת
בו. עם תיקון 10, "בין משחקים" הוא לובי, אז שם אפשר גם.

- כוונה חדשה `{ t: 'RENAME', key, name }`.
- אימות: `validateJoin(room, name, seatId)` — בדיוק אותו שער, כולל `nameKey`,
  אורך, ריק, וכפילות מול שאר המושבים.
- נדחה כשהחדר נעול (`NOT_ALLOWED`) — כלומר במשחק רץ.
- ה-`GuestSession` השמור מתעדכן, אחרת חיבור מחדש היה מחזיר את השם הישן.
- UI: הקשה על השם שלך ברשימת הלובי הופכת אותו לשדה. אצל המארח ואצל האורח.

---

## 3. בדיקות — הפער האמיתי

`driver`/`view`/`room` מכוסים היטב. `peer.ts`, `useHost.ts`, `useGuest.ts` הם
**אפס בדיקות**, ושם יושבים כל ארבעת הקריטיים. הבדיקות שנכתבו עד כה מדדו את
החלק שכבר היה בסדר.

**`FakeChannel`** — מימוש של `Channel` שאפשר לנהוג בו ידנית:

```ts
type FakeChannel = Channel & {
  /** מה נשלח, לפי הסדר. */
  sent: unknown[];
  /** הודעה שמגיעה מהצד השני. */
  deliver(msg: unknown): void;
  /** הצד השני נעלם בלי לפלוט close — התרחיש שאי אפשר לבדוק בלי זה. */
  goSilent(): void;
  /** סגירה מסודרת מהצד השני. */
  remoteClose(): void;
};
```

| # | בדיקה |
|---|---|
| 1 | `close()` **לא** מפעיל `onClose`; `drop()` כן |
| 2 | `onClose` שנרשם אחרי סגירה נקרא מיד |
| 3 | ערוץ שהשתתק ולא פלט `close` נסרק אחרי הזמן, כולל ערוץ **בלי מושב** |
| 4 | `REJECTED` סופי סוגר את הערוץ |
| 5 | לולאת הברוקר: `disconnected` → `reconnect` בהשהיה עולה; `open` חוזר מאפס אותה |
| 6 | `'open'` שני **לא** רושם handler נוסף ל-`connection` (הבאג של ההודעה הכפולה) |
| 7 | `openHost` לא מחזיר peer הרוס מה-cache |
| 8 | שני תקציבי הניסיונות: חיוג ראשון נכשל מהר, חיבור מחדש רודף 90 שניות |
| 9 | `RENAME`: מתקבל בלובי, נדחה בנעילה, נדחה על שם כפול |
| 10 | `REOPEN`: מחזיר ל-SETUP, שומר מושבים, מאפס `seatOrder`, ומאפשר start נוסף |

בדיקות 5–8 דורשות שעון מזויף (`vi.useFakeTimers`) ו-Peer מזויף. הלוגיקה של
לולאת ההתאוששות תחולץ לפונקציה טהורה `reconnectDelay(attempt)` כדי שהחלק
המעניין ייבדק בלי mock בכלל.

---

## 4. סדר הביצוע

| שלב | תוכן | למה בסדר הזה |
|---|---|---|
| A | `FakeChannel` + תשתית הבדיקות | קודם הרשת שתתפוס את התיקונים |
| B | תיקון 1 (`wrap`) + תיקון 4 (`openHost` cancel) | הכי קטנים, הכי חוסמים. פותחים את `npm run dev` |
| C | תיקון 2 + 3 (ברוקר וחשיפתו) | הגדול. תלוי ב-B |
| D | תיקון 5 + 6 (תקציבים, רקע) | צד האורח |
| E | תיקון 7 + 8 (ערוצי רפאים, סגירה מסודרת) | ניקיון |
| F | תיקון 9 + 10 + 11 (UX) | עצמאיים, בסוף |

---

## 5. מה לא נעשה, ולמה

| | |
|---|---|
| **החלפת PeerJS** | לא מתקנת אף אחד מהכשלים — כולם בקוד שלנו. Trystero הוא mesh ולא id-dial, בלי TURN, ומחייב לבנות מחדש את "המארח מחזיק קוד". `Channel` נשאר תפר נקי, אז החלפה מאוחרת היא יום עבודה |
| **Cloudflare Durable Objects** | היחיד שהיה מסלק גם את הברוקר וגם את הטלפון כנקודות כשל, וקיים ב-free tier. אבל rewrite גדול מכל המסמך הזה יחד. לשקול רק אם הברוקר יתגלה כלא אמין **אחרי** התיקונים |
| **מיגרציית מארח** | מכניסה split-brain כמחלקת כשל חדשה. במשחק שבו טלפון אחד יודע מי המתחזה, זה גרוע מהתקלה שהיא מונעת |
| **TURN בתשלום** | אין שירות חינמי ואמין להישען עליו. ה-TURN של PeerJS מכסה חלק. הפתרון הנכון הוא זיהוי + הודעה ספציפית: "ודאו ששני הטלפונים על אותה רשת wifi, ולא על רשת אורחים" |
